function App() {
  return (
    <div className="app-container">
      <header className="app-header">
        <h1>Storybook + CI Feedback Loop</h1>
        <p>A proof-of-concept demonstrating visual regression testing in frontend pipelines.</p>
      </header>
      <main className="app-main">
        <p>Launch Storybook to inspect components:</p>
        <code>npm run storybook</code>
      </main>
    </div>
  );
}

export default App;
